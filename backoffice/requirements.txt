# ORM imposé par le sujet
sqlalchemy==2.0.36
# Pilote PostgreSQL (traducteur) utilisé par SQLAlchemy (cf. postgresql+psycopg:// dans DATABASE_URL)
psycopg[binary]==3.2.3
# Hachage de mots de passe
argon2-cffi==23.1.0
# Framework web du Backoffice (SSR + routing)
flask==3.1.3
# Client HTTP pour interroger l'API produits externe
requests==2.34.2
# Gestion des sessions authentifiées (cookie signé, user_loader)
flask-login==0.6.3
